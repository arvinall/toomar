/**
 * @example
 * ```js
 * uncleanEdges() // -> { cleanEdges: false }
 * ```
 * 
 * @returns an object with `cleanEdges` property that is equal to `false`
 * 
 * @see {@link configs/clean-edges.cleanEdges}
 */
export function uncleanEdges (): { cleanEdges: false }
