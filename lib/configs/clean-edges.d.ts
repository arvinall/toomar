/**
 * @example
 * ```js
 * cleanEdges() // -> { cleanEdges: true }
 * ```
 * 
 * @returns an object with `cleanEdges` property that is equal to `true`
 * 
 * @see {@link configs/unclean-edges.uncleanEdges}
 */
export function cleanEdges (): { cleanEdges: true }
