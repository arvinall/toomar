/**
 * @example
 * ```js
 * coverEdges() // -> { coverEdges: true }
 * ```
 * 
 * @returns an object with `coverEdges` property that is equal to `true`
 * 
 * @see {@link configs/uncover-edges.uncoverEdges}
 */
export function coverEdges (): { coverEdges: true }
